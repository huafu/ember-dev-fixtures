export default {
  ///**
  // * Used to filter when a `find` is called with a hash as parameter
  // *
  // * @method queryFixtures
  // * @param {Array} fixtures
  // * @param {Object} query
  // * @returns {Array}
  // */
  //queryFixtures: function (fixtures, query) {
  //  return fixtures.filter(/*...*/)
  //}
};
