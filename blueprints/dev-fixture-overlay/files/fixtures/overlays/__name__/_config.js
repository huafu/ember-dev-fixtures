export default {
  //You can include other overlays too by defining them in the `include` property.
  // the based fixture will be merged with all overlays in `include` in order
  // and finally this overlay will be merged
  //include: ['other-overlay-1', 'other-overlay-2']
};
